import { db } from '../../../lib/db'; import { requireUser } from '../../../lib/auth'; import { unauthorized } from '../../../lib/http'; import { NextResponse } from 'next/server'; export async function GET(){try{const u=await requireUser();if(u.role!=='LANDLORD')return unauthorized();return NextResponse.json({subscriptions:await db.subscription.findMany({where:{landlordId:u.sub},orderBy:{startsAt:'desc'}})})}catch{return unauthorized()}}
